version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gocritic
    - misspell
    - revive

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclusions:
    generated: strict
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gocritic
          - revive

run:
  timeout: 5m
  tests: true
